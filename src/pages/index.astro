---
import Layout from "../layouts/Layout.astro";
---

<Layout pageTitle="Home Page">
  <section class="flex min-h-[60vh] items-center justify-center px-4">
    <div class="w-full max-w-3xl space-y-5">
      <div
        class="flex w-fit items-center gap-2 rounded-full border border-secondary/50 px-3 py-1 text-sm font-medium text-text/70"
      >
        <div class="relative flex size-1.5">
          <span
            class="absolute inline-flex h-full w-full animate-ping-slow rounded-full bg-accent/50"
          ></span>
          <span class="relative inline-flex size-1.5 rounded-full bg-accent"
          ></span>
        </div>
        <span>Available for projects</span>
      </div>

      <div class="flex items-center justify-between">
        <h1 class="text-4xl font-extrabold text-primary sm:text-5xl">
          Hey, I'm Ngoc.
        </h1>
      </div>

      <p class="text-base leading-7 text-balance text-text/70">
        I tinker with code, Linux, and systems — stuff breaks, I fix it, and
        share what I pick up along the way. To me, tech’s best when it’s
        playful, digestible, and always a trade-off.
      </p>
    </div>
  </section>

  <section id="recent-posts" class="flex min-h-screen justify-center"></section>
</Layout>
